node_modules/
dist/
.server-build/
*.tsbuildinfo
.DS_Store

# Headless sim save files / reports
.sim/

# Playwright / debug scratch output
.playwright-mcp/
.playwright-cli/
output/
outputs/

# Sim report JSONs are regeneratable machine output — keep the .md write-ups, not the raw reports.
docs/sim/*.json

# Local agent/editor config
.claude/

# Hero art candidates — large binaries; force-add the chosen final asset only
assets/hero-v2/
