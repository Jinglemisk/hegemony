ICONOGRAPHY / ASSET AUDIT — /Users/jinglemisk/Desktop/hegemony (branch feat/phase1-currencies)

=== 1. FAMILIES (details in families array; mixed-fidelity notes in issues fields) ===
Eight distinct families, 65 files total under /Users/jinglemisk/Desktop/hegemony/assets/. dist/ contains only Vite-hashed build copies (no strays needing action); output/ contains only a browser/ dir (no images). Every generated raster family was clearly produced as black-or-color art on WHITE, then either (a) thresholded into white-silhouette *-mask.png files consumed via CSS mask-image + tint, or (b) shipped as-is with the paper background baked in.

Key consumption architecture (all in src):
- ResourceIcon (src/components/Sprites.tsx:80) renders material resources as CSS masks (src/styles/map.css:193-221) tinted by --resource-color from src/ui/resourceVisuals.ts (wood #354927, stone #8f8571, gold #d98a35, food #9bbf52, influence #1f6977, happiness #2f7d46). Happiness bypasses the mask system and uses 5 self-colored theatre-mask PNGs (src/styles/resources.css:53-83) keyed to value thresholds (Sprites.tsx:101-119).
- AtlasIcon / TerrainSprite / UiSprite (Sprites.tsx) cut cells out of the three codex-banana-showcase atlas PNGs via background-position percentages (src/styles/sprites.css); atlases are wired as CSS vars --icon-atlas/--terrain-atlas/--ui-atlas in src/styles/base.css:131-134.
- Settlement masks are CSS vars --capital-glyph/--city-glyph/--colony-glyph (base.css:135-137); the full-color settlement PNGs are UNUSED.
- Event cards: 28/28 webp mapped 1:1 in src/components/board/events.ts EVENT_CARD_ART.
- Season icons: SeasonDial.tsx imports all 4. Map board: HexMap.tsx:66. Map-mode SVGs: HexMap.tsx:71-76. greek-frame.png: border-image on the intel panel (src/styles/intel.css:19). light-hero-tableau.png: layered page background (base.css:150).

=== 2. CONSISTENCY MATRIX ===
Axes of clash: rendering style (engraved line-art vs solid glyph vs painted vector vs painterly), background (transparent vs baked parchment vs baked checkerboard), palette (terracotta/parchment vs teal vs self-colored), and dimensionality (flat vs shaded 3D — user has a stated flat-UI preference).

- resource masks × codex icon-atlas: CLASH. Same concepts (wood/stone/gold/food) exist twice in two styles — masks render as flat single-color silhouettes (modern icon-font look once tinted), the atlas versions are dark-red engraved line-art with cream fill on parchment. Both appear in the UI simultaneously (AtlasIcon vs ResourceIcon).
- happiness theatre masks × everything: CLASH. Self-colored modern flat vector (emerald green / brick red with soft shading), no terracotta ink line, no parchment; sits inside resource rows next to tinted silhouettes. Its green/red valence palette also fights the "color = owner" hex grammar.
- season icons × event cards: CLASH. Seasons are fully-colored ornamental folk-vector (olive-green tree, orange sun-face) while event cards are strict sepia/terracotta duotone engraving; they appear near each other (SeasonDial + season event cards).
- season icons internal: CLASH. winter.webp is a dimensional engraved-metal blue-silver snowflake with baked drop shadow; spring/summer/autumn are flat warm-palette vector. Winter reads as a different game.
- settlement full-color PNGs × flat direction: CLASH (moot — unused). Cartoon 3D clay rendering with baked fake-transparency checkerboard; only the derived masks ship.
- aegean-sea-board × terracotta system: DELIBERATE CONTRAST. Teal painterly antique-map sea with hand-drawn ships/wind-heads/compass; anchored by --aegean #1f6977 so it's token-tied, but it is the only painterly texture-heavy family besides the hero tableau.
- map-mode SVGs × generated art: MILD CLASH. Crisp hand-authored flat SVG badges using exact palette tokens — more "app UI" than "ceramic artifact", but the only assets guaranteed on-palette.
- codex ui-kit atlas × its own consumers: SEMANTIC CLASH. The 3x3 sheet (owl, hourglass, cards, amphora, wreath, helmet, meander, vote-hand, sun/moon) is repurposed via aliases in Sprites.tsx:48-65 (endTurn→primary cell, growAction→seasonMarker cell, victoryPoint→voteToken cell) — icons frequently don't mean what they show.
- Backgrounds: event cards, codex atlases, greek-frame, hero tableau, board all have baked opaque paper/texture; masks are transparent; seasons mostly transparent (winter suspect). Baked-paper sprite cells produce visible cream squares on the live parchment theme.

=== 3. CONCEPTS WITH NO ICON (exist in src/game/types.ts / Phase 1) ===
- Bank / market exchange (BankRates, TradableMaterial, types.ts:253-255): MarketTab.tsx reuses ResourceIcon masks only; no bank, exchange-rate, or drachma/coin-purse icon distinct from the gold stack.
- Civic calm action (calmUsedThisTurn, types.ts:344): CalmModal.tsx has zero iconography.
- Ladder (ladderUsedThisTurn; LadderModal.tsx): borrows city/colony atlas cells; no ladder/ascent glyph.
- Ventures (ventureUsedThisTurn; VentureModal.tsx): no icon at all.
- Riot / riot tiers (RiotTier, pending riot, types.ts:226-229; RiotModal.tsx): no icon. Ironically an unrest-mask.png/unrest-source.png pair EXISTS but "unrest" is not a Resource — and map.css:218-220 points .resourceMask-happiness at unrest-mask.png (naming/semantic mismatch).
- Event tables (EventTableId: riot | merchantConvoy | grandEmbassy | colonistsVoyage, types.ts:186; EventTableModal.tsx): four flavorful table identities, zero art.
- Standalone pop-type icons (citizens/freemen/slaves), building icons (marketplace/temple/workshop/granary), and terrain icons exist ONLY as cells inside the baked-parchment codex atlases — no clean transparent standalone versions.
- Victory metrics (cities/pops/citizens/stockpile/happiness) and deck backs (eventDeck/seasonDeck/resolutionDeck/victoryPoint) are all UiSprite aliases to unrelated atlas cells.

=== 4. PIPELINE NOTES ===
Evidence: ~/CLAUDE.md documents the Nano Banana (Gemini) CLI (~/bin/banana "prompt" -o dir -n name) plus ~/bin/remove-bg for background removal; git history shows "Add Codex brand showcase assets" (637be62), "Add Nano Banana showcase assets" (914b1f1), "perf(assets): convert event-card art to WebP (Tier 2)" (fe35640), "feat(ui): interface overhaul, inline-icon system, painted season art" (f6196b2); docs/reference/codex-banana-showcase.html is a showcase page for the atlas batch. The *-source.png → *-mask.png pairs indicate a threshold/invert derivation step (1024 black-glyph source → 512 white silhouette); settlement masks were derived at odd sizes (720/560/673) suggesting manual cropping. The baked checkerboard in settlement PNGs and baked parchment in atlases show remove-bg was NOT applied to those batches.
A consistent regeneration batch would need: (1) one locked style prompt — terracotta-ink engraved line-art matching the event cards/icon atlas (#722420/#8f2e13 ink range on #f4e6c8 parchment), flat, single line weight; (2) explicit "isolated on plain white background, no paper texture, no drop shadow" then ~/bin/remove-bg --replace; (3) fixed canvas sizes per role (1024 source → 512 mask, 320 chrome icons, 832x1248 cards) and a scripted mask-derivation step; (4) one border treatment for cards (drought uses art-deco corners, market-day uses meander — pick one); (5) coverage list for the Phase 1 gaps above; (6) regenerate winter to match the other three seasons; (7) split atlases into individual transparent sprites so cells stop carrying cream squares; (8) move unused intermediates (*-source.png, full-color settlement PNGs) out of assets/.

## TOP FINDINGS
- Two competing icon languages ship simultaneously: flat tinted silhouette masks (ResourceIcon) vs engraved terracotta line-art atlas cells (AtlasIcon) for the SAME concepts (wood/stone/gold/food), plus a third self-colored modern style for happiness theatre masks.
- The three codex-banana atlases have parchment paper baked into every cell (not transparent), and Sprites.tsx:48-65 aliases UI semantics to unrelated cells (endTurn→primary button cell, victoryPoint→voteToken, growAction→seasonMarker) — the UI sprite layer is effectively placeholder.
- Phase 1 currencies have zero dedicated iconography: bank/market, civic calm, ladder, ventures, riot, and the four event tables (riot/merchantConvoy/grandEmbassy/colonistsVoyage) all render icon-less or borrow unrelated art; meanwhile an orphaned unrest-mask.png exists and map.css:218 misuses it as the happiness mask.
- winter.webp breaks the season set (dimensional metallic blue-silver engraving with baked shadow vs flat warm folk-vector spring/summer/autumn), and event cards use two different border treatments (art-deco corners vs meander frame).
- Six *-source.png intermediates and three full-color settlement PNGs (with fake-transparency checkerboard baked in, ~1MB each) are committed but never referenced; settlement masks also have inconsistent canvases (720/560/673px) risking misaligned glyphs.
- Pipeline is reproducible: Nano Banana CLI + remove-bg documented in ~/CLAUDE.md, mask-derivation convention already established, and the event-card/icon-atlas engraved-terracotta style is the obvious target style for a unifying regeneration batch (highest-fidelity, most on-brand family).

## FAMILIES
{"family": "resource-icons (material sources + masks)", "count": 12, "format": "PNG", "dimensions": "6 sources 1024x1024, 6 masks 512x512", "styleDescriptor": "Bold solid-black pictogram glyphs on white (isometric log bundle, coin stack with laurel emboss, scroll-in-wreath), heavy uniform fills, icon-font weight; masks are white-silhouette inversions. Once tinted by resourceVisuals.ts they read as flat modern single-color icons.", "fidelity": "good", "usedWhere": "Masks only: ResourceIcon (Sprites.tsx:98) via .resourceMask-* mask-image in map.css:193-221, tinted with --resource-color. All *-source.png files are unused intermediates. unrest-mask serves the happiness class (map.css:218) despite unrest not being a Resource.", "issues": "Sources committed but unused (~1.6MB); unrest/happiness naming mismatch; style clashes with engraved atlas versions of the same concepts; no distinct drachmae/bank mask."}
{"family": "resource-icons (happiness theatre masks)", "count": 5, "format": "PNG", "dimensions": "256x256", "styleDescriptor": "Flat modern vector Greek theatre masks, self-colored (emerald green happy, muted brick-red angry) with subtle shading and white cutout features, transparent bg, no outline ink; contemporary app-icon feel rather than ceramic/engraved.", "fidelity": "good", "usedWhere": "ResourceIcon happiness branch (Sprites.tsx:89-96) -> .happinessTheatreIcon-* background-images (resources.css:53-83); sized in ledger.css.", "issues": "Only self-colored family \u2014 bypasses the tint system; green/red valence palette and modern style clash with both the silhouette masks and the terracotta engraving language."}
{"family": "settlement-icons", "count": 6, "format": "PNG", "dimensions": "full-color 1024x1024; masks 720x720 / 560x560 / 673x673", "styleDescriptor": "Full-color versions: cartoon 3D clay-render terracotta objects (faceted star, gate, ship-emblem flag on pole) with dark outlines and a FAKE transparency checkerboard baked into the pixels. Masks: clean white silhouettes on transparent.", "fidelity": "rough", "usedWhere": "Masks only, as --capital-glyph/--city-glyph/--colony-glyph CSS vars (base.css:135-137) for map settlement markers. Full-color PNGs unused (~2.7MB dead weight).", "issues": "Fidelity is split: masks are good, full-color versions rough (baked checkerboard makes them unusable). Mask canvases inconsistent (720/560/673) so relative glyph scale is luck; 3D style contradicts flat-UI preference (moot while unused)."}
{"family": "season-icons", "count": 4, "format": "WebP", "dimensions": "320x320", "styleDescriptor": "Ornamental colored folk-vector emblems: spring = swirling olive tree (olive greens/browns), summer = sun-face with spiral rays (ochre/rust), autumn similar warm flat style; winter = ornate engraved SILVER-BLUE snowflake with metallic gradients and baked soft shadow \u2014 dimensional, off-palette outlier.", "fidelity": "good", "usedWhere": "SeasonDial.tsx:7-10 (topbar season dial), each paired with an accent color (winter mapped to var(--aegean)).", "issues": "Winter is only rough-fit: breaks flatness and palette, possible non-transparent halo; colored style clashes with the duotone event-card engraving shown alongside."}
{"family": "event-cards", "count": 28, "format": "WebP", "dimensions": "832x1248 (2:3 portrait)", "styleDescriptor": "Sepia/terracotta duotone engraved line-art on aged parchment: fine hatched linework, Greek scenes (agora crowds, cracked drought earth, temples, owl banners), warm rust wash shading, decorative border \u2014 either full meander frame or art-deco corner frame depending on card. Cohesive, evocative, print-quality.", "fidelity": "high", "usedWhere": "EVENT_CARD_ART in src/components/board/events.ts:9-36, 1:1 coverage of all 10 season + 18 player events; rendered in event modals/deck UI.", "issues": "Two different border treatments across the set; baked parchment bg (acceptable for full-bleed cards); largest payload (~6.3MB total, already WebP)."}
{"family": "codex-banana-showcase (atlases + frame + tableau)", "count": 5, "format": "PNG", "dimensions": "icon-atlas 2048x2048 (4x4), ui-kit 2048x2048 (3x3), terrain-atlas 1254x1254 (2x2), greek-frame 2048x2048, hero-tableau 1536x1024", "styleDescriptor": "Icon atlas: dark-red engraved line icons with cream fill on aged parchment (logs, stone, coins, bread, wreath, theatre masks, citizens, freeman, kneeling slave, market stall, temple, anvil, granary, capital tower, city gate, coastal colony hut). UI kit: thinner terracotta line symbols (owl, hourglass, card fan, amphora, wreath, helmet, meander maze, voting hand, sun/moon) on parchment. Terrain atlas: 4 hex-framed brick-red stencil emblems (mountain, olive hill, forest, wheat plains) on cream \u2014 strongest, most on-brand pieces. greek-frame: terracotta panel with woven texture noise and cream meander key border. hero-tableau: busy isometric clay-toned illustration of hex tiles with temples/coins/handshakes.", "fidelity": "good", "usedWhere": "base.css:131-134 CSS vars consumed by AtlasIcon/TerrainSprite/UiSprite via sprites.css percentage cells across ~14 components (ledger tabs, modals, scoreboard, command panels); greek-frame = intel panel border-image (intel.css:19); hero-tableau = page background layer (base.css:150).", "issues": "Fidelity mixed: icon/terrain atlases good, ui-kit semantics and frame compression rough. Parchment baked into every sprite cell (no transparency); percentage-grid slicing fragile; UiSprite cell aliasing makes icons semantically wrong; greek-frame is a 1.9MB PNG used only as a border; atlases cannot be tinted per-owner."}
{"family": "map (sea board)", "count": 1, "format": "PNG", "dimensions": "2400x1792 (3.8MB)", "styleDescriptor": "Painterly antique nautical chart: deep teal wave-textured sea, parchment margin with gilded scrollwork frame, hand-engraved triremes, wind-blowing face medallions, sea serpent, dolphin, compass rose in copper/tan inks.", "fidelity": "high", "usedWhere": "HexMap.tsx:66 as the SVG board backdrop (SEA_BACKDROP_HREF).", "issues": "Only teal-dominant asset \u2014 intentional sea contrast but palette-isolated; heavy PNG (WebP candidate)."}
{"family": "map-modes", "count": 2, "format": "SVG (hand-authored)", "dimensions": "96x96 viewBox", "styleDescriptor": "Crisp flat vector toggle badges: rounded dark squircle chrome, cream inner card, hex motif filled with exact theme tokens (#f4e6c8/#d98a35/#354927/#9bbf52/#1f6977), gradient strokes, drop-shadow filter. App-UI aesthetic, perfectly on-palette.", "fidelity": "good", "usedWhere": "HexMap.tsx:71-76 map-mode toggle buttons; terrain-mode CSS support in sprites.css:166-170.", "issues": "Only hand-coded vector family \u2014 stylistic island vs generated engraved art; drop shadows slightly against flat preference."}
